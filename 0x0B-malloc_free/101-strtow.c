#include "main.h"
#include <stdlib.h>

/**
 * strtow - split a string into words
 * @str: pointer to char
 * Return: pointer to an array of strings(words)
 *		each element of the array is a string null-terminated (word)
 *	NULL if str == NULL or str = "" or it not enough memory
 */
char **strtow(char *str)
{

}
